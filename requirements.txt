# --- Core AI & Vector Storage ---
# Primary vector database (Disk-based, Zero-Copy)
lancedb>=0.4.0
# Lightweight, hardware-aware embedding generation (Uses ONNX Runtime)
fastembed>=0.3.1

# --- Data Processing & Math ---
# Pinned < 2.0.0 to ensure compatibility with Numba/ONNX libraries
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

# --- Document Ingestion ---
# Extract text from PDFs
pdfminer.six>=20231228
# Extract text from DOCX files
python-docx>=1.1.2

# --- User Interface (GUI) ---
customtkinter>=5.2.2
# Image handling for UI icons/previews
Pillow>=10.3.0
# Standard Tkinter wrapper (usually built-in, but good to ensure environment checks)
tk-tools>=0.1.0

# --- System Utilities ---
# For global hotkey detection (Ctrl+K)
keyboard>=0.13.5
# Progress bars for file indexing
tqdm>=4.66.4
# HTTP requests for downloading models
requests>=2.31.0

# --- Search & Ranking ---
# BM25 library for optional lexical search support
rank-bm25>=0.2.2
